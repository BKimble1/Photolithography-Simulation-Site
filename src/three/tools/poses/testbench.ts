import type { ToolPose } from '../../poses';

/** Camera pose(s) for the TestBench scene (metres). Variant poses override the default. */
export const POSE: ToolPose = { pos: [0.5, 1.35, 0.6], target: [0, 1.0, 0] };
