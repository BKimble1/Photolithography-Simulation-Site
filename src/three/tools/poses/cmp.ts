import type { ToolPose } from '../../poses';

/** Camera pose(s) for the Cmp scene (metres). Variant poses override the default. */
export const POSE: ToolPose = { pos: [1.2, 1.75, 1.45], target: [0, 0.95, 0] };
