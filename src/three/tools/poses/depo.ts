import type { ToolPose } from '../../poses';

/** Camera pose(s) for the Depo scene (metres). Variant poses override the default. */
export const POSE: ToolPose = { pos: [1.25, 1.65, 1.6], target: [0, 1.0, 0] };
