import type { ToolPose } from '../../poses';

/** Camera pose(s) for the Implant scene (metres). Variant poses override the default. */
export const POSE: ToolPose = { pos: [2.6, 2.1, 3.1], target: [0.2, 1.0, 0] };
