import type { ToolPose } from '../../poses';

/** Camera pose(s) for the Furnace scene (metres). Variant poses override the default. */
export const POSE: ToolPose = { pos: [1.9, 1.7, 2.6], target: [0, 1.25, 0] };
