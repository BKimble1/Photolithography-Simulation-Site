import type { ToolPose } from '../../poses';

/** Camera pose(s) for the Inspect scene (metres). Variant poses override the default. */
export const POSE: ToolPose = { pos: [0.95, 1.45, 1.35], target: [0, 1.0, 0] };
