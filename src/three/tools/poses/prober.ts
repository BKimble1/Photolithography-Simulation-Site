import type { ToolPose } from '../../poses';

/** Camera pose(s) for the Prober scene (metres). Variant poses override the default. */
export const POSE: ToolPose = { pos: [1.1, 1.55, 1.35], target: [0, 1.0, 0] };
