import type { ToolPose } from '../../poses';

/** Camera pose(s) for the Dicing scene (metres). Variant poses override the default. */
export const POSE: ToolPose = { pos: [0.85, 1.4, 1.1], target: [0, 1.0, 0] };
